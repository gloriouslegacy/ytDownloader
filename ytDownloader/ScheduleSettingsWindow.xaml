<Window
    x:Class="ytDownloader.ScheduleSettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{DynamicResource Dialog_AutoScheduleSettings}"
    Width="600"
    Height="820"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource WindowBackgroundBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}"
    ResizeMode="NoResize">

    <Window.Resources>
        <!--  Button 스타일  -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PressedBackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  TextBlock  -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        </Style>

        <!--  ComboBox  -->
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton"
                                          Grid.Column="2"
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{DynamicResource BorderBrush}"
                                          BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="20" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Name="ContentSite"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      Margin="5,3,5,3"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      TextBlock.Foreground="{TemplateBinding Foreground}" />
                                    <Path Name="Arrow"
                                          Grid.Column="1"
                                          Fill="{TemplateBinding Foreground}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                                </Grid>
                            </ToggleButton>
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource PrimaryBackgroundBrush}"
                                            BorderThickness="1"
                                            BorderBrush="{DynamicResource BorderBrush}">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleButton" Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ComboBoxItem  -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
            <Setter Property="Padding" Value="5,3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <ContentPresenter TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  CheckBox  -->
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border
                                Name="Border"
                                Width="16"
                                Height="16"
                                Background="{DynamicResource PrimaryBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3">
                                <Path
                                    Name="CheckMark"
                                    Width="10"
                                    Height="10"
                                    Data="M 0 5 L 3 8 L 8 0"
                                    Stretch="Uniform"
                                    Stroke="{DynamicResource PrimaryTextBrush}"
                                    StrokeThickness="2"
                                    Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter
                                Margin="5,0,0,0"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True"
                                TextElement.Foreground="{TemplateBinding Foreground}" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  설명  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,15"
            TextWrapping="Wrap"
            Foreground="{DynamicResource PrimaryTextBrush}">
            <Run FontWeight="Bold" Text="{DynamicResource Dialog_AutoScheduleSettings}" />
            <LineBreak />
            <Run Text="{DynamicResource Label_AutoScheduleDescription}" />
        </TextBlock>

        <!--  실행 주기  -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock
                Margin="0,0,0,5"
                FontWeight="Bold"
                Foreground="{DynamicResource PrimaryTextBrush}"
                Text="{DynamicResource Label_FrequencyDays}" />
            <ComboBox Name="cmbFrequency" />
        </StackPanel>

        <!--  실행 시간  -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock
                Margin="0,0,0,5"
                FontWeight="Bold"
                Foreground="{DynamicResource PrimaryTextBrush}"
                Text="{DynamicResource Label_ExecutionTimeSettings}" />
            <StackPanel Orientation="Horizontal">
                <ComboBox Name="cmbHour" Width="100" SelectedIndex="2" />
                <TextBlock
                    Margin="10,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="{DynamicResource Label_Hour}" />
                <ComboBox Name="cmbMinute" Width="100" SelectedIndex="0" />
                <TextBlock
                    Margin="10,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="{DynamicResource Label_Minute}" />
            </StackPanel>
        </StackPanel>

        <!--  스케줄러 개별 설정  -->
        <Border Grid.Row="3" Margin="0,0,0,10" Padding="10" Background="{DynamicResource PrimaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,10"
                    FontWeight="Bold"
                    FontSize="14"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="{DynamicResource Label_SchedulerDownloadSettings}" />

                <!--  채널 URL  -->
                <TextBlock
                    Margin="0,5,0,5"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="{DynamicResource Label_ChannelUrl}" />
                <DockPanel Margin="0,0,0,10">
                    <Button
                        DockPanel.Dock="Right"
                        Width="70"
                        Click="btnPasteChannelUrl_Click"
                        Content="{DynamicResource Button_Paste}"
                        ToolTip="{DynamicResource ToolTip_PasteChannelUrl}" />
                    <TextBox
                        Name="txtSchedulerChannelUrl"
                        Height="26"
                        VerticalContentAlignment="Center"
                        ToolTip="{DynamicResource ToolTip_SchedulerChannelUrl}" />
                </DockPanel>

                <!--  저장 경로  -->
                <TextBlock
                    Margin="0,5,0,5"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="{DynamicResource Label_SavePath}" />
                <DockPanel Margin="0,0,0,10">
                    <Button
                        DockPanel.Dock="Right"
                        Width="70"
                        Click="btnBrowseSavePath_Click"
                        Content="{DynamicResource Button_Browse}"
                        ToolTip="{DynamicResource ToolTip_Browse}" />
                    <TextBox
                        Name="txtSchedulerSavePath"
                        Height="26"
                        VerticalContentAlignment="Center"
                        IsReadOnly="True"
                        ToolTip="{DynamicResource ToolTip_SchedulerSavePath}" />
                </DockPanel>

                <!--  포맷 선택  -->
                <TextBlock
                    Margin="0,5,0,5"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="{DynamicResource Label_Format}" />
                <ComboBox
                    Name="cmbSchedulerFormat"
                    Margin="0,0,0,10"
                    ToolTip="{DynamicResource ToolTip_Format}">
                    <ComboBoxItem Content="{DynamicResource Format_BestVideo}" />
                    <ComboBoxItem Content="{DynamicResource Format_Video1080p}" />
                    <ComboBoxItem Content="{DynamicResource Format_Video720p}" />
                    <ComboBoxItem Content="{DynamicResource Format_Video480p}" />
                    <ComboBoxItem Content="{DynamicResource Format_AudioMP3}" />
                    <ComboBoxItem Content="{DynamicResource Format_AudioBest}" />
                    <ComboBoxItem Content="{DynamicResource Format_AudioFLAC}" />
                </ComboBox>

                <!--  자막 설정  -->
                <CheckBox
                    Name="chkSchedulerSubtitle"
                    Margin="0,5,0,5"
                    Content="{DynamicResource CheckBox_Subtitle}"
                    ToolTip="{DynamicResource ToolTip_Subtitle}" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <ComboBox
                        Name="cmbSchedulerSubtitleFormat"
                        Width="120"
                        ToolTip="{DynamicResource ToolTip_SubtitleFormat}">
                        <ComboBoxItem Content="srt" />
                        <ComboBoxItem Content="vtt" />
                        <ComboBoxItem Content="ass" />
                    </ComboBox>
                    <ComboBox
                        Name="cmbSchedulerSubtitleLang"
                        Width="120"
                        Margin="10,0,0,0"
                        ToolTip="{DynamicResource ToolTip_SubtitleLang}">
                        <ComboBoxItem Content="ko" />
                        <ComboBoxItem Content="en" />
                        <ComboBoxItem Content="ja" />
                        <ComboBoxItem Content="zh-Hans" />
                        <ComboBoxItem Content="zh-Hant" />
                        <ComboBoxItem Content="fr" />
                        <ComboBoxItem Content="de" />
                        <ComboBoxItem Content="es" />
                        <ComboBoxItem Content="ru" />
                        <ComboBoxItem Content="pt" />
                        <ComboBoxItem Content="it" />
                        <ComboBoxItem Content="ar" />
                        <ComboBoxItem Content="hi" />
                        <ComboBoxItem Content="th" />
                        <ComboBoxItem Content="vi" />
                    </ComboBox>
                </StackPanel>

                <!--  알림 및 최대 다운로드 개수  -->
                <CheckBox
                    Name="chkSchedulerNotification"
                    Margin="0,5,0,10"
                    Content="{DynamicResource CheckBox_EnableNotification}"
                    ToolTip="{DynamicResource ToolTip_EnableNotification}" />

                <DockPanel Margin="0,0,0,5">
                    <TextBlock
                        DockPanel.Dock="Left"
                        Margin="0,0,10,0"
                        VerticalAlignment="Center"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource PrimaryTextBrush}"
                        Text="{DynamicResource Label_MaxDownloads}" />
                    <TextBox
                        Name="txtSchedulerMaxDownloads"
                        Width="80"
                        ToolTip="{DynamicResource ToolTip_SchedulerMaxDownloads}" />
                </DockPanel>

                <Button
                    Width="100"
                    Margin="0,10,0,0"
                    HorizontalAlignment="Right"
                    Click="btnRegister_Click"
                    Content="{DynamicResource Button_Register}" />
            </StackPanel>
        </Border>

        <!--  등록된 스케줄 목록  -->
        <StackPanel Grid.Row="4" Margin="0,0,0,10">
            <TextBlock
                Margin="0,0,0,5"
                FontWeight="Bold"
                Foreground="{DynamicResource PrimaryTextBrush}"
                Text="{DynamicResource Label_RegisteredScheduleList}" />
        </StackPanel>

        <!--  스케줄 목록 ListBox  -->
        <Border
            Grid.Row="5"
            Background="{DynamicResource PrimaryBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Margin="0,0,0,10">
            <ListBox
                Name="lstScheduledTasks"
                Background="Transparent"
                BorderThickness="0"
                Foreground="{DynamicResource PrimaryTextBrush}"
                SelectionMode="Single"
                MinHeight="80" />
        </Border>

        <!--  버튼  -->
        <StackPanel
            Grid.Row="6"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="5"
                Click="btnDeleteSelected_Click"
                Content="{DynamicResource Button_DeleteSelected}" />
            <Button
                Width="100"
                Margin="5"
                Click="btnDeleteAll_Click"
                Content="{DynamicResource Button_DeleteAll}" />
            <Button
                Width="100"
                Margin="5"
                Click="btnRefresh_Click"
                Content="{DynamicResource Button_Refresh}" />
            <Button
                Width="100"
                Margin="5"
                Click="btnClose_Click"
                Content="{DynamicResource Button_Close}" />
        </StackPanel>
    </Grid>
</Window>
