<Window
    x:Class="ytDownloader.ScheduleSettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="자동 예약 설정"
    Width="600"
    Height="820"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource WindowBackgroundBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}"
    ResizeMode="NoResize">

    <Window.Resources>
        <!--  Button 스타일  -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PressedBackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  TextBlock  -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        </Style>

        <!--  ComboBox  -->
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton"
                                          Grid.Column="2"
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{DynamicResource BorderBrush}"
                                          BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="20" />
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Name="ContentSite"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      Margin="5,3,5,3"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      TextBlock.Foreground="{TemplateBinding Foreground}" />
                                    <Path Name="Arrow"
                                          Grid.Column="1"
                                          Fill="{TemplateBinding Foreground}"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                                </Grid>
                            </ToggleButton>
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource PrimaryBackgroundBrush}"
                                            BorderThickness="1"
                                            BorderBrush="{DynamicResource BorderBrush}">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleButton" Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ComboBoxItem  -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Background" Value="{DynamicResource PrimaryBackgroundBrush}" />
            <Setter Property="Padding" Value="5,3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <ContentPresenter TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  CheckBox  -->
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border
                                Name="Border"
                                Width="16"
                                Height="16"
                                Background="{DynamicResource PrimaryBackgroundBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="3">
                                <Path
                                    Name="CheckMark"
                                    Width="10"
                                    Height="10"
                                    Data="M 0 5 L 3 8 L 8 0"
                                    Stretch="Uniform"
                                    Stroke="{DynamicResource PrimaryTextBrush}"
                                    StrokeThickness="2"
                                    Visibility="Collapsed" />
                            </Border>
                            <ContentPresenter
                                Margin="5,0,0,0"
                                VerticalAlignment="Center"
                                RecognizesAccessKey="True"
                                TextElement.Foreground="{TemplateBinding Foreground}" />
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  설명  -->
        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,15"
            TextWrapping="Wrap"
            Foreground="{DynamicResource PrimaryTextBrush}">
            <Run FontWeight="Bold">자동 예약 다운로드 설정</Run>
            <LineBreak />
            <Run>예약된 채널들을 지정한 시간에 자동으로 다운로드합니다.</Run>
        </TextBlock>

        <!--  실행 주기  -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock
                Margin="0,0,0,5"
                FontWeight="Bold"
                Foreground="{DynamicResource PrimaryTextBrush}"
                Text="실행 주기 (일)" />
            <ComboBox Name="cmbFrequency" />
        </StackPanel>

        <!--  실행 시간  -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock
                Margin="0,0,0,5"
                FontWeight="Bold"
                Foreground="{DynamicResource PrimaryTextBrush}"
                Text="실행 시간" />
            <StackPanel Orientation="Horizontal">
                <ComboBox Name="cmbHour" Width="100" SelectedIndex="2" />
                <TextBlock
                    Margin="10,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="시" />
                <ComboBox Name="cmbMinute" Width="100" SelectedIndex="0" />
                <TextBlock
                    Margin="10,0"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="분" />
            </StackPanel>
        </StackPanel>

        <!--  스케줄러 개별 설정  -->
        <Border Grid.Row="3" Margin="0,0,0,10" Padding="10" Background="{DynamicResource PrimaryBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,10"
                    FontWeight="Bold"
                    FontSize="14"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="스케줄러 다운로드 설정" />

                <!--  채널 URL  -->
                <TextBlock
                    Margin="0,5,0,5"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="채널 URL" />
                <DockPanel Margin="0,0,0,10">
                    <TextBox
                        Name="txtSchedulerChannelUrl"
                        Width="380"
                        ToolTip="특정 채널 URL을 입력하면 해당 채널만 다운로드됩니다" />
                    <Button
                        Width="70"
                        Click="btnPasteChannelUrl_Click"
                        Content="붙여넣기"
                        ToolTip="클립보드의 URL 붙여넣기" />
                </DockPanel>

                <!--  저장 경로  -->
                <TextBlock
                    Margin="0,5,0,5"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="저장 경로" />
                <DockPanel Margin="0,0,0,10">
                    <TextBox
                        Name="txtSchedulerSavePath"
                        Width="380"
                        IsReadOnly="True"
                        ToolTip="이 스케줄러에서 다운로드한 파일이 저장될 경로" />
                    <Button
                        Width="70"
                        Click="btnBrowseSavePath_Click"
                        Content="찾기"
                        ToolTip="저장 경로 변경" />
                </DockPanel>

                <!--  포맷 선택  -->
                <TextBlock
                    Margin="0,5,0,5"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource PrimaryTextBrush}"
                    Text="포맷 선택" />
                <ComboBox
                    Name="cmbSchedulerFormat"
                    Margin="0,0,0,10"
                    ToolTip="다운로드할 영상/음악 품질 선택">
                    <ComboBoxItem Content="영상 (최고화질)" />
                    <ComboBoxItem Content="영상 (1080p)" />
                    <ComboBoxItem Content="영상 (720p)" />
                    <ComboBoxItem Content="영상 (480p)" />
                    <ComboBoxItem Content="음악 (MP3 V0 VBR)" />
                    <ComboBoxItem Content="음악 (Best 원본유지)" />
                    <ComboBoxItem Content="음악 (FLAC 무손실 변환)" />
                </ComboBox>

                <!--  자막 설정  -->
                <CheckBox
                    Name="chkSchedulerSubtitle"
                    Margin="0,5,0,5"
                    Content="자막 다운로드"
                    ToolTip="영상과 함께 자막 파일 다운로드" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <ComboBox
                        Name="cmbSchedulerSubtitleFormat"
                        Width="120"
                        ToolTip="자막 파일 형식 (srt: 범용, vtt: 웹용, ass: 고급 스타일)">
                        <ComboBoxItem Content="srt" />
                        <ComboBoxItem Content="vtt" />
                        <ComboBoxItem Content="ass" />
                    </ComboBox>
                    <ComboBox
                        Name="cmbSchedulerSubtitleLang"
                        Width="120"
                        Margin="10,0,0,0"
                        ToolTip="자막 언어 선택">
                        <ComboBoxItem Content="ko" />
                        <ComboBoxItem Content="en" />
                        <ComboBoxItem Content="ja" />
                        <ComboBoxItem Content="zh-Hans" />
                        <ComboBoxItem Content="zh-Hant" />
                        <ComboBoxItem Content="fr" />
                        <ComboBoxItem Content="de" />
                        <ComboBoxItem Content="es" />
                        <ComboBoxItem Content="ru" />
                        <ComboBoxItem Content="pt" />
                        <ComboBoxItem Content="it" />
                        <ComboBoxItem Content="ar" />
                        <ComboBoxItem Content="hi" />
                        <ComboBoxItem Content="th" />
                        <ComboBoxItem Content="vi" />
                    </ComboBox>
                </StackPanel>

                <!--  알림 및 최대 다운로드 개수  -->
                <CheckBox
                    Name="chkSchedulerNotification"
                    Margin="0,5,0,10"
                    Content="다운로드 완료 알림"
                    ToolTip="다운로드 완료 시 알림 표시" />

                <DockPanel Margin="0,0,0,5">
                    <TextBlock
                        DockPanel.Dock="Left"
                        Margin="0,0,10,0"
                        VerticalAlignment="Center"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource PrimaryTextBrush}"
                        Text="최대 다운로드 개수 (0은 무한)" />
                    <TextBox
                        Name="txtSchedulerMaxDownloads"
                        Width="80"
                        ToolTip="채널당 다운로드할 최대 영상 개수 (0은 무한)" />
                </DockPanel>

                <Button
                    Width="100"
                    Margin="0,10,0,0"
                    HorizontalAlignment="Right"
                    Click="btnRegister_Click"
                    Content="등록" />
            </StackPanel>
        </Border>

        <!--  등록된 스케줄 목록  -->
        <StackPanel Grid.Row="4" Margin="0,0,0,10">
            <TextBlock
                Margin="0,0,0,5"
                FontWeight="Bold"
                Foreground="{DynamicResource PrimaryTextBrush}"
                Text="등록된 스케줄 목록" />
        </StackPanel>

        <!--  스케줄 목록 ListBox  -->
        <Border
            Grid.Row="5"
            Background="{DynamicResource PrimaryBackgroundBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Margin="0,0,0,10">
            <ListBox
                Name="lstScheduledTasks"
                Background="Transparent"
                BorderThickness="0"
                Foreground="{DynamicResource PrimaryTextBrush}"
                SelectionMode="Single"
                MinHeight="80" />
        </Border>

        <!--  버튼  -->
        <StackPanel
            Grid.Row="6"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Width="100"
                Margin="5"
                Click="btnDeleteSelected_Click"
                Content="선택 삭제" />
            <Button
                Width="100"
                Margin="5"
                Click="btnDeleteAll_Click"
                Content="전체 삭제" />
            <Button
                Width="100"
                Margin="5"
                Click="btnRefresh_Click"
                Content="새로고침" />
            <Button
                Width="100"
                Margin="5"
                Click="btnClose_Click"
                Content="닫기" />
        </StackPanel>
    </Grid>
</Window>
