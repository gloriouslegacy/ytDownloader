<Window
    x:Class="ytDownloader.DefenderWarningWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="{DynamicResource Dialog_DefenderWarning}"
    Width="500"
    Height="300"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource WindowBackgroundBrush}"
    Foreground="{DynamicResource PrimaryTextBrush}"
    ResizeMode="NoResize">

    <Window.Resources>
        <!--  Button 스타일  -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" TextBlock.Foreground="{TemplateBinding Foreground}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{DynamicResource PressedBackgroundBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  TextBlock  -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Warning Icon and Title  -->
        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,20"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <TextBlock
                FontSize="24"
                FontWeight="Bold"
                Text="⚠️" />
            <TextBlock
                Margin="10,0,0,0"
                FontSize="18"
                FontWeight="Bold"
                Text="{DynamicResource Dialog_DefenderWarning}" />
        </StackPanel>

        <!--  Warning Message  -->
        <TextBlock
            Grid.Row="1"
            Margin="0,0,0,20"
            FontSize="14"
            Text="{DynamicResource Label_DefenderWarningMessage}"
            TextWrapping="Wrap" />

        <!--  Download Link Button  -->
        <Button
            x:Name="btnDownloadExclusion"
            Grid.Row="2"
            Height="40"
            Margin="0,0,0,20"
            VerticalAlignment="Top"
            Click="btnDownloadExclusion_Click"
            Content="{DynamicResource Button_DownloadExclusion}"
            FontSize="13" />

        <!--  Action Buttons  -->
        <StackPanel
            Grid.Row="3"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                x:Name="btnContinue"
                Width="120"
                Height="35"
                Click="btnContinue_Click"
                Content="{DynamicResource Button_Continue}"
                FontSize="14" />
            <Button
                x:Name="btnCancel"
                Width="120"
                Height="35"
                Click="btnCancel_Click"
                Content="{DynamicResource Button_Cancel}"
                FontSize="14" />
        </StackPanel>
    </Grid>
</Window>
